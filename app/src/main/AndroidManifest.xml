<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcotrackApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegistroResiduoActivity"
            android:label="Registrar Residuo"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ResiduosActivity"
            android:label="Lista de Residuos"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".VehiculosActivity"
            android:label="Vehículos Recolectores"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CentroReciclajeActivity"
            android:label="Centro de Reciclaje"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EstadisticasActivity"
            android:label="Estadísticas"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ZonasActivity"
            android:label="Zonas Urbanas"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".GraficosActivity"
            android:label="Gráficos"
            android:parentActivityName=".EstadisticasActivity" />


    </application>

</manifest>